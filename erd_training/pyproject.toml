[build-system]
requires = ["setuptools>=42"]
build-backend = "setuptools.build_meta"

[project]
name = "erd-training"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = ["torch>=2.0", "tqdm"]

[project.scripts]
erd-train = "erd_training.train:main"
