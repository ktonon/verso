# ogma_symbolic

Core symbolic mathematics library for Ogma.

Provides expression parsing, algebraic simplification, LaTeX rendering, dimensional analysis, and SI unit system. Used by `ogma_doc` for inline math verification and by `ogma_training` for learned simplification.
